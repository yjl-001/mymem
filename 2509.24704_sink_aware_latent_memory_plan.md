# Sink-aware latent memory 插入策略实验方案

关联论文：MemGen: Weaving Generative Latent Memory for Self-Evolving Agents  
论文编号：2509.24704  
文档目的：将“基于 attention sink 判断 latent memory 插入时机”的想法整理为可执行、可验证的实验方案。

## 1. 背景与问题

MemGen 的核心机制是通过 Weaver 生成 latent memory，并通过 Trigger 决定是否在生成过程中插入 latent memory。根据当前代码实践，Weaver 训练阶段通常还没有可用的 Trigger，因此插入位置主要依赖规则：

- prompt 计算完成后默认插入一次 latent memory；
- decode 阶段在前若干个 delimiter 位置插入 latent memory；
- 之后再训练 Trigger 来学习是否调用 latent memory。

这种两阶段训练方式可能导致一个偏置：Weaver 更熟悉 completion 前半段的插入分布，后续 Trigger 即使被强化学习训练，也容易继承这种前置调用偏好。

因此，本方案尝试引入 decoder-only LLM 中的 attention sink 现象，用它辅助判断 latent memory 的插入时机。

## 2. 核心研究假设

本文档中的核心假设不是 MemGen 论文已有结论，而是一个待验证的研究假设：

> 当 decoder-only LLM 在语义边界处把异常高比例的注意力分配给 sink token 时，说明当前推理缺少明确的内容锚点；此时插入 latent memory，可以为后续生成提供更有信息量的注意力对象。

更形式化地说，如果当前 step \(j\) 对 sink token 集合 \(S_{\text{sink}}\) 的注意力质量异常升高：

\[
SinkMass(j)=
\frac{1}{|H_{\text{sink}}|}
\sum_{(l,h)\in H_{\text{sink}}}
\sum_{k\in S_{\text{sink}}}
A^{l,h}_{j,k}
\]

则可以将其视为 latent memory 插入的候选信号。

需要注意：

- 高 sink attention 不必然表示模型“不需要上下文”；
- 它更稳妥的解释是：当前 attention 没有明显集中到具体内容 token，而是更多流向默认锚点；
- latent memory 插入通常发生在当前 token 之后，因此主要影响后续 token，而不是已经生成的当前 token。

## 3. 总体路线

推荐路线如下：

```text
校准 sink tokens / sink heads
-> 在线计算 SinkMassZ
-> 用 sink-aware 分布训练 Weaver
-> 用 sink-aware candidate 约束 Trigger
-> 做同预算 ablation 验证效果
```

其中：

- sink 负责提供结构性信号；
- boundary 负责约束插入必须发生在合理的语义边界；
- uncertainty 负责判断当前推理是否不稳定；
- Trigger 负责在候选位置上做任务相关的动态开关。

## 4. 指标定义

### 4.1 sink received score

用于离线校准哪些 token 位置是 sink：

\[
SinkReceived(k)=
\frac{1}{LH}
\sum_{l,h}
\frac{1}{|\{q:q>k+\Delta\}|}
\sum_{q>k+\Delta}
A^{l,h}_{q,k}
\]

其中：

- \(k\)：候选 key 位置；
- \(q\)：后续 query 位置；
- \(l\)：layer；
- \(h\)：attention head；
- \(\Delta\)：局部排除窗口，用于避免把局部 attention 误判为 sink；
- \(A^{l,h}_{q,k}\)：第 \(l\) 层第 \(h\) 个 head 中，位置 \(q\) 对位置 \(k\) 的 attention。

需要做位置归一化，避免越靠前的位置天然被更多后续 token 看到。

### 4.2 online sink mass

用于在线判断当前 step 是否正在把 attention 流向 sink：

\[
SinkMass(j)=
\frac{1}{|H_{\text{sink}}|}
\sum_{(l,h)\in H_{\text{sink}}}
\sum_{k\in S_{\text{sink}}}
A^{l,h}_{j,k}
\]

为了排除不同样本、不同长度、不同层头之间的量纲差异，推荐使用标准化分数：

\[
SinkMassZ(j)=
\frac{SinkMass(j)-\mu_{\text{sink}}}
{\sigma_{\text{sink}}}
\]

### 4.3 触发分数

推荐将 sink signal 作为 trigger 的一部分，而不是唯一条件：

\[
Score(j)=
\alpha SinkMassZ(j)
+
\beta EntropyZ(j)
+
\gamma Boundary(j)
-
\lambda Budget(j)
\]

其中：

- \(SinkMassZ(j)\)：当前 step 是否异常关注 sink；
- \(EntropyZ(j)\)：next-token 分布是否不确定；
- \(Boundary(j)\)：当前位置是否为 delimiter、换行、推理步骤边界、代码块边界等；
- \(Budget(j)\)：调用预算惩罚，避免频繁插入。

## 5. 阶段 0：复现原始基线

### 目标

确认原始 MemGen 插入策略的性能和位置分布，为后续改动提供基线。

### 实验设置

对比以下策略：

| 策略 | 描述 |
|---|---|
| Original MemGen | prompt 后插入一次，decode 阶段在前 \(K\) 个 delimiter 插入 |
| Random delimiter | 在 delimiter 中随机选择插入位置 |
| All delimiter | 每个 delimiter 都插入，受预算限制 |
| Original Trigger | 使用原始 Trigger 学习是否插入 |

### 记录指标

- 任务 accuracy 或 reward；
- 平均 latent memory 调用次数；
- 插入位置分布；
- early / middle / late 调用比例；
- 同调用预算下的性能。

### 预期结果

- Original MemGen 的插入位置会明显偏向 completion 前半段；
- Original Trigger 可能优于 random，但仍可能继承前置调用偏好；
- 该阶段应产出 baseline 表格和插入位置直方图。

### 失败解释

如果 Original MemGen 并不前置，说明当前代码版本、数据集或 delimiter 规则已经改变，需要重新检查插入逻辑。

## 6. 阶段 1：校准 sink tokens 和 sink heads

### 目标

找出当前 base LLM 中哪些 token、哪些 layer/head 承担 attention sink 功能。

### 实验设置

1. 准备校准集：
   - 数学推理；
   - 代码生成；
   - 问答；
   - 长上下文对话；
   - 与 MemGen 训练任务分布一致的样本。
2. 使用 teacher forcing 跑模型。
3. 开启 `output_attentions=True`。
4. 统计每个 key 位置的 `SinkReceived(k)`。
5. 对 score 做位置归一化。
6. 使用 KV cache ablation 做功能验证。

### ablation

| 删除对象 | 目的 |
|---|---|
| top sink tokens | 检查删除 sink 是否明显损害模型 |
| random early tokens | 排除“只是因为位置靠前”的解释 |
| random content tokens | 对比普通上下文 token 的影响 |
| delimiter tokens | 检查 delimiter 是否也承担局部 sink 功能 |

### 记录指标

- perplexity / loss 变化；
- 生成质量变化；
- top sink token 类型；
- sink-sensitive layer/head 分布；
- 不同任务类型下 sink 是否稳定。

### 预期结果

- BOS、system prompt 开头、assistant header、前几个 token 可能是全局 sink；
- 某些 delimiter 或换行可能成为局部 sink；
- 删除 top sink tokens 比删除随机 tokens 造成更明显退化；
- 最终得到：

```text
S_sink = {sink token positions or token types}
H_sink = {sink-sensitive layers and heads}
```

### 失败解释

如果删除 top sink tokens 和删除随机 tokens 影响接近，说明该模型的 sink 功能不强，或者当前 sink 指标没有捕捉到真正的功能性 sink。

## 7. 阶段 2：验证 online sink mass 是否能预测 memory 需求

### 目标

验证 `SinkMassZ(j)` 是否能作为 latent memory 调用时机的信号。

### 实验设置

在模型生成过程中，仅记录不插入：

1. 在 delimiter、换行、步骤边界处记录 `SinkMassZ(j)`；
2. 同时记录 next-token entropy；
3. 记录当前轨迹最终 reward；
4. 标记正确轨迹和错误轨迹；
5. 对比高 `SinkMassZ` 位置是否集中在推理转折点或错误前区域。

### 分析维度

| 分析项 | 问题 |
|---|---|
| `SinkMassZ` vs reward | 高 sink 是否更常出现在低 reward 轨迹 |
| `SinkMassZ` vs entropy | sink 升高是否伴随不确定性升高 |
| `SinkMassZ` vs boundary | sink 升高是否发生在语义边界 |
| `SinkMassZ` vs position | 是否仍然强烈偏向前缀 |

### 预期结果

- 单独 `SinkMassZ` 可能不够稳定；
- `SinkMassZ + entropy + boundary` 应该更能预测需要 memory 的位置；
- 如果高 sink 只出现在前缀附近，则不能直接作为 online trigger，需要引入位置分桶和前缀惩罚。

### 失败解释

如果 `SinkMassZ` 与 reward、entropy、边界都没有相关性，说明 sink 更适合作为结构性位置先验，而不是动态调用信号。

## 8. 阶段 3：改造 Weaver 训练插入分布

### 目标

让 Weaver 学会在更广泛、更合理的推理位置上生成 latent memory，而不是只适应前 \(K\) 个 delimiter。

### 训练策略

保留 prompt 后默认插入：

```text
prompt 后固定插入 1 次 latent memory
```

decode 阶段使用混合采样：

```text
60%-70%：选择 SinkMassZ 高的 boundary
20%-30%：选择 random delimiter
10%：强制选择 late-position boundary
```

同时使用相对位置分桶：

```text
0%-25%
25%-50%
50%-75%
75%-100%
```

每个 bucket 内选择 top sink-aware candidate。

### 实验设置

对比以下 Weaver 训练方式：

| 训练方式 | 描述 |
|---|---|
| Original Weaver | prompt 后一次 + 前 \(K\) 个 delimiter |
| Random Weaver | prompt 后一次 + random delimiter |
| Sink-aware Weaver | prompt 后一次 + sink-aware boundary |
| Bucketed Sink-aware Weaver | prompt 后一次 + 分桶 sink-aware boundary |

### 记录指标

- 训练 reward；
- 训练稳定性；
- 不同位置段插入时的 loss/reward；
- middle/late 插入是否可用；
- 相同调用预算下的 downstream accuracy。

### 预期结果

- Sink-aware Weaver 的插入位置分布应更均匀；
- middle/late 插入的效果应优于 Original Weaver；
- 同调用预算下，Sink-aware Weaver 不应低于 Original Weaver，最好更高。

### 失败解释

如果 Sink-aware Weaver 效果下降，可能说明：

- sink candidate 质量不高；
- Weaver 对位置分布变化敏感；
- late insertion 的 latent memory 难以影响最终答案；
- 需要逐步 anneal，从 original 分布过渡到 sink-aware 分布。

## 9. 阶段 4：sink-only 插入策略

### 目标

在不使用 Trigger 的情况下，验证 sink signal 本身是否能带来收益。

### 策略

只在满足以下条件的位置插入：

```text
boundary = true
SinkMassZ(j) > threshold
entropy is high or not low
memory budget not exhausted
cooldown satisfied
```

### 实验设置

对比策略：

| 策略 | 描述 |
|---|---|
| Original first-K delimiter | 原始规则插入 |
| Random delimiter | 随机 delimiter 插入 |
| Static sink position | 固定在校准出的 sink 位置插入 |
| Online sink mass | 根据当前 SinkMassZ 插入 |
| Sink + entropy + boundary | 多信号联合插入 |

所有策略需要控制相同平均插入次数。

### 记录指标

- accuracy/reward；
- 平均调用次数；
- 同预算性能；
- 插入位置分布；
- 插入后 next-N-token loss；
- 插入后 sink mass 是否下降。

### 预期结果

- Static sink position 可能表现一般，因为容易退化到前缀；
- Online sink mass 应优于 random delimiter；
- `Sink + entropy + boundary` 应最稳定；
- 如果同调用次数下优于 Original first-K delimiter，说明 sink signal 本身有效。

### 失败解释

如果 sink-only 不优于 random，说明 sink signal 不能直接决定插入时机，需要 Trigger 学习更复杂的状态依赖。

## 10. 阶段 5：加入 sink-aware Trigger

### 目标

让 Trigger 不再从全空间学习插入位置，而是在 sink-aware candidate 上做动态开关。

### 方案 A：candidate filter

先用规则筛出候选位置：

```text
candidate(j) =
boundary(j)
and SinkMassZ(j) is high
and budget not exhausted
```

然后只在这些候选位置上询问 Trigger 是否插入。

### 方案 B：logit prior

将 sink signal 作为 Trigger 的 logit prior：

\[
trigger\_logit(j)
=
f_{\phi}(h_j)
+
\alpha SinkMassZ(j)
+
\beta EntropyZ(j)
\]

### 实验设置

对比以下方法：

| 方法 | 描述 |
|---|---|
| Original Trigger | 原始 Trigger |
| Sink-only | 不使用 Trigger，只使用 sink 规则 |
| Candidate-filter Trigger | 只在 sink-aware candidate 上触发 |
| Logit-prior Trigger | 将 sink signal 注入 Trigger logit |

### 记录指标

- accuracy/reward；
- 平均调用次数；
- 同预算性能；
- 插入位置分布；
- Trigger action entropy；
- Trigger 是否仍然偏向 completion 前半段；
- Trigger 是否能筛掉无效 sink candidate。

### 预期结果

- Sink-aware Trigger 应该比 Original Trigger 更少依赖前半段；
- 同等调用次数下，Sink-aware Trigger 应优于 Original Trigger；
- 如果 Sink-aware Trigger 优于 Sink-only，说明 Trigger 学到了任务相关的残差决策；
- 如果两者接近，说明 sink rule 已经覆盖了大部分有效决策。

### 失败解释

如果 Sink-aware Trigger 不如 Original Trigger，可能说明 candidate filter 过窄，限制了 Trigger 的探索空间。此时应优先尝试 logit prior，而不是硬过滤。

## 11. 阶段 6：机制验证

### 目标

证明插入 latent memory 后，模型真的在使用 memory，而不是只是因为插入次数或位置变化带来偶然收益。

### 实验设置

对比以下条件：

| 条件 | 描述 |
|---|---|
| Normal latent memory | 正常 Weaver 生成 latent memory |
| Random latent memory | 插入随机 latent memory |
| Zero latent memory | 插入全零 latent memory |
| No latent memory | 不插入 memory |
| Delayed latent memory | 将同一个 memory 延后若干 token 插入 |

### 记录指标

- 插入后后续 token 对 latent memory slot 的 attention；
- 插入前后 `SinkMassZ` 变化；
- next-N-token loss；
- 最终 reward；
- 不同层/head 是否从 sink 转向 latent memory。

### 预期结果

- Normal latent memory 应显著优于 random/zero/no memory；
- 插入后，后续 token 对 latent memory slot 的 attention 应上升；
- 插入后，部分 attention 应从 prefix sink 转移到 latent memory；
- Delayed latent memory 的效果应弱于及时插入。

### 失败解释

如果 normal/random/zero memory 效果接近，说明收益可能来自插入扰动或位置先验，而不是 Weaver 生成的有效 latent memory。

## 12. 最小可行实验

如果资源有限，可以先做一个 MVP：

1. 复现 Original MemGen baseline；
2. 在校准集上找 top sink tokens/head；
3. 只实现 online `SinkMassZ + boundary` 插入；
4. 不训练 Trigger，只做 sink-only；
5. 控制平均调用次数，对比 first-K delimiter 和 random delimiter；
6. 画出插入位置分布和最终 reward。

MVP 的成功标准：

```text
在相同平均调用次数下：
  SinkMassZ + boundary > first-K delimiter
  或 SinkMassZ + boundary > random delimiter
```

如果 MVP 不成立，再进入 Trigger 改造的收益会比较有限。

## 13. 最终成功标准

推荐使用同预算标准，而不是只看绝对 accuracy：

```text
在相同平均 memory 调用次数下：
  sink-aware Weaver + sink-aware Trigger
  >
  Original MemGen Trigger
```

或者：

```text
在相同 reward/accuracy 下：
  sink-aware 方法使用更少 latent memory 调用
```

如果性能提升来自调用次数暴涨，则不能说明插入时机更合理。

## 14. 主要风险

| 风险 | 说明 | 应对 |
|---|---|---|
| sink 过度前缀化 | sink 高分集中在 BOS/开头 token | 使用位置分桶和前缀惩罚 |
| sink 不是 memory need | sink 只是结构习惯，不代表需要信息 | 联合 entropy 和 boundary |
| Trigger 被候选集限制 | filter 太窄导致错过有效位置 | 优先尝试 logit prior |
| Weaver 不适应新位置 | Weaver 只会在早期位置工作 | 改造 Weaver 训练分布 |
| attention 获取成本高 | full attention matrix 显存开销大 | 只记录选定层/head，或离线校准后在线近似 |
| 插入后不可影响当前 token | 当前 step 已经生成完成 | 在 boundary 后插入，影响后续生成 |

## 15. 推荐实验顺序

```text
Step 0: 原始 baseline + 位置分布
Step 1: sink 校准 + KV ablation
Step 2: online SinkMassZ 相关性分析
Step 3: sink-only 插入实验
Step 4: sink-aware Weaver 训练
Step 5: sink-aware Trigger 训练
Step 6: latent memory 使用机制验证
```

## 16. 结论

该方案的关键不是简单地把 latent memory 插入到 sink position，而是将 attention sink 从一个静态观察现象转化为动态插入信号：

```text
当模型在语义边界处把过多 attention 流向 sink，
且当前预测不确定时，
插入 latent memory，
为后续推理提供新的内容锚点。
```

因此，最推荐的最终设计是：

```text
sink calibration 提供结构性先验
online SinkMassZ 提供动态时机信号
boundary/entropy/budget 提供约束
Weaver 通过 sink-aware 分布学习在这些位置写入 memory
Trigger 在 sink-aware candidates 上做任务相关的开关决策
```

这一路线能够直接回应 MemGen 当前两阶段训练中的前置插入偏置，并且每一步都可以通过独立实验验证。
